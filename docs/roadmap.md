# AI-readiness plan

Execution history for the machine-facing layer — all six phases are done. Rationale, benchmarks, and scope boundaries live in [`ai-readiness.md`](./ai-readiness.md).

**This file is history, not a to-do list.** For live, actionable work, open [`backlog.md`](./backlog.md) instead — that's the session anchor now. This file still gets a new Session log row when something ships (see `backlog.md`'s protocol), and its State block still gets updated when the overall status changes, but there is no "current phase" to pick up here.

**2026-09-02 note:** feature specs referenced below (`mcp-server-spec.md`, `compound-component-docs-spec.md`, `layered-filtering-spec.md`, `docs-site-spec.md`) now live in [`/specs`](../specs), not `/docs` — moved as part of adopting the ai-product-starter-kit's `/decisions` + `/specs` convention (see [`/decisions`](../decisions)). Links below point to the new location; older prose mentions of `docs/<name>-spec.md` in the session log are left as written, since that's a dated historical record.

**2026-09-07 note:** this file now carries only the State block, condensed phase checklists, and the session log — the Session protocol and the two backlog items moved to [`backlog.md`](./backlog.md); full task-by-task reasoning, every bug found, and the complete session log are in [`roadmap-archive.md`](./roadmap-archive.md).

---

## State

```
Last updated:   2026-09-07 (roadmap/backlog split — see 2026-09-07 session
                log entry; overall project status below is unchanged since
                2026-09-02)
Current phase:  All six phases (0–5) of this plan are done, plus the MCP
                server — scoped out of this plan at Phase 0 ("deliberately
                out of scope"), picked back up as a separate spec
                (specs/mcp-server-spec.md) once a second consumer became a
                real near-term possibility, all 7 of its own phases shipped
                2026-09-01. DS.one 3/5 → 4/5, Kaelig 5/10 → 6/10 shipped —
                both re-scored against live evidence. That evidence now
                includes something mcp-server-spec.md's own State block had
                left open since its Phase 1: this session ran `/mcp` in a
                real Claude Code session on 2026-09-02 and saw
                `amezquita-design-system` connected, alongside 2 other
                servers (3 connected total) — the first confirmation from
                an actual client connection, not a throwaway SDK-handshake
                script. docs/ai-readiness-audit.md is written and committed
                but not posted anywhere external — still the one remaining
                decision, and it's the user's to make, not a task to pick
                up unprompted; it now understates the score by one signal
                on each benchmark and would need the MCP row added before
                being cited again. Also on 2026-09-02: the two out-of-scope
                doc-generator gaps this file logged under Task 4.4 (Card's
                compound sub-components undocumented; Table's usage example a
                Storybook placeholder) are now fixed — see
                docs/compound-component-docs-spec.md — closed via a separate,
                self-contained spec rather than reopening this file's phases.
                Also on 2026-09-02, later the same day: DataTable gained
                column-level filtering and an app-level toolbar slot — see
                docs/layered-filtering-spec.md, another separate,
                self-contained spec, closed end to end (audit, open
                questions resolved and logged, implementation, a live-browser
                review gate since this repo has no unit-test framework, two
                clean cold-test runs, docs regenerated). Its own cold test
                surfaced one new, real, out-of-scope finding, logged here
                rather than fixed in that spec: get_component("DataTable")'s
                doc doesn't surface the `T extends Record<string, unknown>`
                generic constraint on the `render` prop's signature, which
                a plain row interface fails against unless it carries an
                index signature — pre-existing, unrelated to filtering,
                worth a future build-component-docs.mjs pass. Also on
                2026-09-02, separately from the AI-readiness work above:
                adopted the ai-product-starter-kit's governance conventions
                repo-wide — not a phase of this plan, but it changes where
                things in this plan point. New: /decisions (ADR format, 3
                ADRs written — the base/portfolio brand split, rejecting
                transitions.dev as a library while keeping its audit
                technique, and the new line-height lint rule), docs/
                architecture.md and docs/quality.md (real content, not
                templates — the token-tier system, the build pipeline, the
                8 lint rules, what npm run validate actually checks), and
                contributor-skills/ (workflow skills for developing this
                repo — kept separate from the generated, consumer-facing
                skills/amezquita-design-system/ so npm run tokens can't
                collide with it). The four specs this plan already
                referenced (mcp-server-spec.md, compound-component-
                docs-spec.md, layered-filtering-spec.md, docs-site-spec.md)
                moved from docs/ to the new /specs — every link in this
                file pointing at them was updated to the new path; a note
                after this file's own intro paragraph explains the move for
                anyone following an old link. One real, unrelated bug
                surfaced and fixed along the way: check-components-doc.mjs
                printed "29 components documented" — technically correct
                (it counts directories, including internal-only BaseSheet)
                but read as contradicting the "28 public components" this
                plan and AGENTS.md both quote. Fixed to report both numbers
                explicitly ("29 directories documented (28 public + 1
                internal)") rather than one raw figure a reader has to
                reconcile themselves. npm run validate confirmed clean
                after the fix.
Next action:    Nothing required to finish the plan as scoped. If the user
                wants the audit to do its job (the "evidence" return named
                in ai-readiness.md §6), the next step is deciding where it
                gets posted externally (LinkedIn, a portfolio case study
                page, etc.) — a publishing/positioning decision, not a
                coding task. Separately: the MCP server is local-only,
                single-maintainer, stdio-only today — if a second real
                consumer (a team) materializes, mcp-server-spec.md's
                "Local → hosted" section names the two changes that needs
                (swap stdio for HTTP/SSE, add read-only auth) and neither
                is started.
Blocked on:     nothing
```

**Phase 0 headline: 1/5 on the DesignSystems.one signals, 1/10 shipped (+1 partial) on the Kaelig affordances.** The foundation scores well and the distribution layer scores near zero — which is exactly the shape the strategy predicted. Four correctness issues were found during the audit and are listed under the scorecard; fix those before quoting any number publicly.

## Phase status

| Phase | Outcome | Effort | Status |
|---|---|---|---|
| 0 | Baseline self-audit recorded | 1 h | **Done — 2026-08-10** |
| 1 | Compiled machine-readable docs layer | 1 weekend | **Done — 2026-08-10, live.** All artifacts confirmed serving from amezquita.dk. |
| 2 | Agent contract + single validate gate | ½ weekend | **Done — 2026-08-10.** Acceptance's fresh-session behavioral test not independently run — see session log. |
| 3 | shadcn-spec public registry | 1 weekend | **Done — 2026-08-10, live.** `npx shadcn add` verified against the real production URL. |
| 4 | Agent skill at `/.well-known/skills/` | ½ weekend | **Done — 2026-08-10, live.** Tested twice against genuinely fresh subagents; confirmed against the real production domain. |
| 5 | Publish the audit | ½ weekend | **Done — 2026-08-10.** `docs/ai-readiness-audit.md` written; not yet posted anywhere external. |
| — | MCP server *(scoped out of this plan at Phase 0; separate spec)* | — | **Done — 2026-09-01, spec complete; connection confirmed live 2026-09-02.** Not one of this plan's numbered phases — Phase 0 explicitly called it out of scope, and it was picked back up later as its own document, [`specs/mcp-server-spec.md`](../specs/mcp-server-spec.md), once a second consumer became a real near-term possibility. Listed here so the phase table isn't misread as a complete list of what shipped. |

**Guardrail: do Phases 0–3. Treat 4 and 5 as optional.** The failure mode is building a bigger affordance surface than the system underneath it.

---

**Full task-by-task reasoning, evidence, and every bug found while building each phase lives in [`roadmap-archive.md`](./roadmap-archive.md).** The lists below are the tasks only — read the archive when you need the *why* behind one.

## Phase 0 — Baseline

Score the system against both benchmarks before changing anything, so the delta is provable later.

- [x] 0.1 Score against the five DesignSystems.one signals.
- [x] 0.2 Score against the ten Kaelig field-study affordances.
- [x] 0.3 Commit the scorecard.

Result: 1/5 DS.one, 1/10 (+1 partial) Kaelig. Four correctness issues found in README claims — all resolved (see archive).

## Phase 1 — Compiled machine-readable docs

Generate `llms.txt`, `llms-full.txt`, per-component `.md` twins, and `tokens.json` from sources that already exist.

- [x] 1.0 Ship `tokens/` in the npm package.
- [x] 1.1 `llms.txt` + `llms-full.txt`.
- [x] 1.2 Per-component `.md` twins (props parsed from `.tsx`, not `components.md` — see archive's design note for why).
- [x] 1.3 `tokens.json` at a stable path.
- [x] 1.3a Fix `token-reference.json` to include all 101 primitives, not just color (was silently missing 83).
- [x] 1.4 Wire into `npm run tokens` + CI staleness check.
- [x] 1.5 Serve from the docs site at canonical paths.

**Acceptance met:** artifacts live at amezquita.dk, deleting a component removes it from every artifact automatically, CI fails on stale output.

## Phase 2 — Agent contract and one gate

- [x] 2.1 `npm run validate` — one command, non-zero exit on any failure.
- [x] 2.2 Fabrication + fallback lint rules (`no-fabricated-token`, `no-token-fallback`).
- [x] 2.3 `AGENTS.md` at root, `CLAUDE.md` symlinked to it.
- [x] 2.4 Point `AGENTS.md` at this roadmap.

**Acceptance:** a fresh agent session, given only "add a Badge component," reads `AGENTS.md`, uses real tokens, runs `validate` unprompted.

## Phase 3 — Public registry

- [x] 3.1 `registry-item.json` per component, shadcn-spec format.
- [x] 3.2 Published at `amezquita.dk/r/<component>.json` + `registry.json` index.
- [x] 3.3 Verified end-to-end: `npx shadcn add` against the live URL produces a correctly themed Button.
- [x] 3.4 Documented in the README.

**Acceptance met:** demoable live from a clean machine.

## Phase 4 — Agent skill *(optional)*

- [x] 4.1 Compact `SKILL.md`, progressive disclosure, links out to the Phase 1 `.md` twins.
- [x] 4.2 "What doesn't exist" prohibition rules naming specific hallucinations.
- [x] 4.3 Served at `/.well-known/skills/`.
- [x] 4.4 Cold-tested twice against fresh subagents with zero memory of the repo — found and fixed two real doc gaps (`SelectGroup`'s shape, an `llms-full.txt` overclaim). Re-test came back clean.

**Acceptance met** — both cold-test runs built successfully first try.

Six doc-generator gaps were found during Phase 4 and the MCP server's own later cold test (Card/Table sub-components, a `SampleTable` placeholder usage example, a DataTable generic-constraint gap). All except the DataTable one are closed — see archive and `specs/compound-component-docs-spec.md`.

## Phase 5 — Publish the audit *(optional)*

- [x] 5.1 Re-scored both benchmarks against live evidence.
- [x] 5.2 Wrote [`ai-readiness-audit.md`](./ai-readiness-audit.md) — the public-facing before/after piece.
- [x] 5.3 Linked 9 verified commits.

Result: DS.one 1/5 → 3/5 (matched the field's top score), Kaelig 1/10 → 5/10 shipped. The MCP server shipped afterward as its own spec, pushing both scores one further (4/5, 6/10) — see the State block above.

---

## Session log

Most recent entries below. **Full log (30 entries back to 2026-08-10) in [`roadmap-archive.md`](./roadmap-archive.md).** New entries get appended here per `backlog.md`'s Session protocol — this file's own copy stays short; older rows can move to the archive in a batch once this section grows past a handful.

| Date | Phase | What changed |
|---|---|---|
| 2026-09-16 | — | **Implemented `decisions/0012` — every static font-size now has a fixed, 4px-grid line-height, on `baseline-grid-vertical-rhythm`, not merged.** Added `line-height.fixed-20/24/28/32/40/60/80` to `tokens/global.json` next to the existing `fixed-16`, redefined `line-height-body` from `1.5` to `28px`, and added `line-height-control/-small/-micro/-h1…-h6/-lead`. `line-height-display`, `-heading` and `-label` are unchanged; the fluid H1–H3 pairings are untouched. **The sweep turned out bigger than the direct references.** `reset.css` puts `line-height-body` on `<body>`, and with a unitless ratio every component that set a font-size but no line-height got 1.5× its own size for free. At a fixed 28px, those components inherit 28px whatever their size, so a 12px table header would have jumped from 18px to 28px. So the sweep covered both groups. Direct references re-pointed: Accordion, Alert, Card, Checkbox, Radio, Switch, Textarea → `control`; Toast → `small`; Tooltip, Input/Textarea hints → `micro`; Dialog title → `lead` (plus `h4` pinned on `.heading.dialog__title`); Heading H4–H6 → `h4`/`h5`/`h6`; EmptyState compact body → `small`, compact title → `h6` (it's a level-4 Heading shrunk to 18px, so without this it would have picked up H4's 32px). Inherited-only rules given an explicit line-height: Select trigger/item → `control`, Select label → `label`, Table body → `control`, Table header/foot/caption → `micro`, Breadcrumb, DataTable count/empty and Hero eyebrow → `small`. 16px control text resolves to 24px exactly as before; everything else moves by 1–2px to the grid. The gaps the ADR's table doesn't cover (12px text, Hero's 20px lead, `<body>`'s unset font-size) are logged in `backlog.md`. `npm run validate` green (260/260); `check-generated-sync` clean against an index with the changes staged. |
| 2026-09-16 | — | **Cross-repo note: portfolio's backlog now tracks the systemic fix for tonight's staleness bugs.** The three separate issues found and fixed this session (the stale-import schema break, the flattened-registry filtering gap, the missing `tokens-manifest.json` entries) all trace back to the same root cause — nothing keeps portfolio's copies of this repo's data in sync automatically, so drift is only ever caught by a human asking to double-check. Rather than re-litigate that here, scoped it as a real item in the portfolio repo's own `docs/backlog.md` ("Stop the design-system docs from going stale silently"): upgrading portfolio past its current `0.3.1` pin (the other half of the 2026-08-12 entry below), a generator for `tokens-manifest.json` instead of hand-maintaining it, and a lightweight CI staleness check comparing portfolio's assumptions against the installed package. None of the three started; this row is just the pointer so a future session here knows where to look. |
| 2026-09-16 | — | **Completeness audit of the loose end from last session: portfolio's `/design-tokens` page (separate from the docs-site spec, unaffected by its axis-key issue).** Traced the page's actual data flow first rather than assuming it shared the other pages' bug — confirmed it renders live `var(--token)` CSS scoped by `data-mode`/`data-expression` wrapper attributes (the portfolio site's own separate theming toggle, `ThemeContext.tsx` + local `styles/brands/{bold,dark,portfolio}-only.css`, unrelated to the design-system package's brand axis), reading only names/labels from a hand-curated `tokens/tokens-manifest.json`, never resolved per-axis values — so the schema break fixed last session never applied here. Audited the manifest's six sections against the actually-deployed CSS (`node_modules/@amezquita/design-system/styles/brands/base-light.css`, confirmed live, not assumed from version number): typography, spacing, motion, radius, and opacity all matched exactly, semantic aliases included. **Almost made a false-positive fix:** the raw `--space-5` through `--space-10` rows looked stale against the Sept 15 space-scale-expansion's renumbering (24/32/48/64/96/128 → 20/24/28/32/40/48) — caught before editing that portfolio is still pinned to the pre-expansion package version, so `base-light.css` actually live on the site still defines the old values; the manifest and the live CSS agree, coincidentally, because neither has moved. Left untouched; folded into the existing "portfolio needs a migration pass" note for when that upgrade happens. **One real, confirmed gap fixed:** `color-curtain`, `color-skeleton-base`, and `color-skeleton-highlight` are all live in the deployed CSS but were missing from the manifest entirely — added `curtain` to the existing Borders group (matching `/design-system/foundations/color`'s own grouping) and a new Skeleton group for the other two, both confirmed against the same live CSS file rather than assumed correct. |
| 2026-09-16 | — | **Human-facing docs site (`specs/docs-site-spec.md`) finished — all five phases now live on amezquita.dk, in the portfolio repo.** Phases 1-4 turned out to already be built (see the follow-up row above), but checking them surfaced a real schema break the import fix alone would have shipped broken: the color axis model changed from an old `light-default`/`light-bold`/`dark-default`/`dark-bold` (mode × expression) system to the current `base-light`/`base-dark`/`portfolio-light`/`portfolio-dark` (brand × mode) system since `decisions/0001`'s brand split, and portfolio's `TokenTable.tsx` (shared by `/tokens` and every `/components/[name]` page), `foundations/color`, `foundations/spacing`, and `foundations/motion` all still hardcoded the old axis keys. Spacing and motion would have crashed outright (`.startsWith()` on `undefined`), not just shown wrong data. Fixed by relabeling to the brand model (user confirmed: show both brands, not just portfolio's) and defaulting single-value displays to `portfolio-light`, since that's the brand this site actually renders in. Also filtered the component registry's newly-flattened sub-components (`parent` field, e.g. `CardBody`) and internal-only entries (`internal: true`, `BaseSheet`) out of the gallery and its static-page generation — both now appear as flat entries in `component-registry.json` since the compound-component-docs work, which the portfolio pages didn't yet account for. Built **Phase 5, the actual new work**: `/design-system/guidelines` — governance (the `npm run validate` gate, the never-violate token rules, the ADR process), versioning (changesets, npm trusted publishing via OIDC), and the AI-agent story (`AGENTS.md` as router, the cold-tested skill, all 7 MCP tools with descriptions) — linking out to this repo's real files (`AGENTS.md`, `decisions/README.md`, `specs/mcp-server-spec.md`) and the live skill file rather than duplicating their content. Linked from the sidebar nav and added as a fifth card on the overview page (its section grid switched from a fixed 4-column layout to `auto-fit` to hold a fifth without a bespoke breakpoint). `specs/docs-site-spec.md` and `backlog.md` both corrected to reflect all five phases done — the backlog entry removed per its own protocol. |
| 2026-09-16 | — | **Follow-up on the 2026-08-12 entry below (portfolio's stale `token-reference.json` import on `/design-system`): resolved.** Checked the portfolio repo directly rather than assumed — `app/design-system/page.tsx` now imports `token-reference.json`/`component-registry.json` from the published `@amezquita/design-system` package, not the local copy that entry flagged as reporting a stale 571 vs. this repo's 620. No PR/commit record of when the fix landed. While checking, found the rest of `specs/docs-site-spec.md`'s Phases 2-4 (landing/pitch, foundations, component gallery) are also already live on amezquita.dk, despite that spec and `backlog.md`'s entry both still reading "not started" — both corrected the same day. Only Phase 5 (Guidelines page) remains open, moved to `backlog.md`. |
| 2026-09-16 | — | **Wrote and implemented `decisions/0010-heading-weight-prop.md`, resolving one of the two `backlog.md` items left open by the Heading migration.** `Heading` gets a `weight?: 'heading' \| 'title'` prop (default `'heading'`, semibold) to replace the three duplicated `.heading.<block>__title { font-weight: var(--font-weight-title) }` compound-selector overrides in `Card.css`/`Dialog.css`/`Drawer.css`. Verified directly against source rather than assumed from memory (`Heading.tsx`/`Heading.css`, all three composition CSS files, and `EmptyState.css`) before writing the ADR: `EmptyState`'s title is the one call site that does *not* get bold — it kept its pre-migration semibold weight byte-identical — so the ADR records that explicitly as a deliberate exception rather than letting the new rule imply it should be bumped to bold later. The weight-assignment rule itself (`Card`/`Dialog`/`Drawer` titles are pinned to the smallest heading tier and sit inside compact container chrome, so they need bold to hold visual presence; standalone headings, already large, stay semibold) is backed by the same Material Design 3 / IBM Carbon precedent `decisions/0008` already cites — MD3 pairs its smaller, supporting type-scale roles with heavier weight, not lighter. Also sets a brand-onboarding requirement (any new brand's typeface must supply both `font-weight-heading` and `font-weight-title`) and governance guidance (swap the typeface first if a brand's font is missing a weight; only revisit the system's own weight-role requirements if that recurs across multiple brands). `backlog.md`'s entry retitled to cover only its remaining open question (whether the original migration needed its own ADR) — still open, needs the user's call. Implemented in the same change on `docs/heading-weight-prop-adr`: `CardTitle` and `BaseSheet.tsx` (shared by `Dialog` and `Drawer`) pass `weight="title"`; the `Card`/`Drawer` overrides are gone and `.heading.dialog__title` keeps only its `font-size` pin, since `AlertDialog` still styles its title through the base `.dialog__title` rule. Verified byte-identical against 41 local Storybook stories (computed title styles and full-page screenshots, before and after). `npm run validate` green (264/264, up from 260). |
| 2026-09-16 | — | **Added a "Voice and tone" section to `AGENTS.md`, closing the last open gap from the context-engineering discussion** (memory architecture shipped earlier the same day as `decisions/0009`). The paragraph drafted in `backlog.md` is pasted in verbatim as its own section, right after "Prefer the MCP tools when connected" and before the hard rules. It sets how an agent should talk when explaining a decision from this system: first person, the real reason stated plainly, the rejected alternative named, and tradeoffs left in instead of smoothed over. Up to now the docs were accurate but had no voice. Backlog entry removed. Docs-only change, on `docs/agents-voice-and-tone`. |
| 2026-09-16 | — | **Code review on the living memory branch found and fixed 6 real issues, 4 of them in the harvester's own signal path.** All reproduced before fixing, all now pinned by tests (`scripts` project 18 → 26). Worst: `sentences()` did not treat a line break as a boundary, so a bullet list merged into one run-on quote and, past the 600-character ceiling, was dropped entirely — reasoning is routinely written as bullets with no terminal punctuation, so a session full of decisions could harvest to **nothing**. Next: only `Edit`/`Write` counted as file edits, making shell-based edits (which this repo's own agent instructions prefer in some modes) invisible — the priority-2 component section could never fire, and the "already wrote an ADR" suppression could be defeated with a heredoc; now also reads `NotebookEdit`/`MultiEdit` and extracts paths from Bash commands that look like writes, with the write-filter on the command rather than the path so reading an ADR doesn't register as authoring one. Also: `isComponentFile` matched `docs/components/` doc twins, so docs-only turns counted as component work; `--harvest` was documented but never parsed, so anything not exactly `--check` fell through to writing (a `--chek` typo silently wrote a draft); `.claude/` became tracked without ignoring `settings.local.json`, so `git add -A` would have swept per-machine permission allowlists in; and both `decisions/0009` and this log's own previous row pointed at a `backlog.md` entry removed on completion per that file's protocol — both now point here instead. The four signal-path bugs share a shape worth naming: a harvester that under-collects fails **silently**, because an empty draft is indistinguishable from a quiet session. Neither `--check` nor testing against real transcripts caught them; reading the code did. A seventh issue surfaced while verifying the fixes, not from the review: the new `scripts` Vitest project inherited the default Vite `cacheDir` rather than getting its own, reproducing the exact cold-cache pre-bundle race `vitest.config.ts`'s own comments document (one run with 4 unrelated files failing to load, the next clean) — fixed and confirmed with two consecutive cold-cache runs. `npm run validate` green (260/260, up from 252); `check-generated-sync` and `changelog-sync --check` both clean. |
| 2026-09-16 | — | **Built the living memory layer — session transcripts now harvest into proposed ADR drafts, on `feat/living-memory-adr-proposals`, not merged.** Closes the `backlog.md` entry of the same name — removed from that file on completion per its own session protocol, so this row is now the record of it. That entry specified the priority order (rejected alternatives first, then component decisions with reasoning, then AI-readiness reasoning) and the hard constraint that a draft never auto-commits. Built as `scripts/propose-decision.mjs` (parses a session transcript, harvests quoted evidence with turn numbers into those three sections, writes `decisions/proposed/session-<date>-<id>.draft.md`) plus a `SessionEnd`/`SessionStart` hook pair in a new `.claude/settings.json` — the repo's first Claude Code hooks, kept out of the existing `hooks/` folder since that one is React hooks (`useButtonWipe.ts`). **Deliberately does not draft prose.** Rejected having the hook call `claude -p`: it costs tokens every session end, spawns Claude from inside a Claude hook, and produces unreviewed prose that reads as finished — a scaffold that obviously needs work invites the review it needs, a polished draft invites a rubber stamp. The writing half is `contributor-skills/decision-proposal/SKILL.md`. **Tuned against real transcripts from this repo rather than assumed, which caught three things review wouldn't have:** (1) requiring reasoning and a component `Edit` in the same turn found *zero* component decisions in a session that made 34 component edits — reasoning rarely shares a turn with the edit it explains, so it now uses a ±2-turn window; (2) the backlog's own example of a component decision ("a prop is a literal union *instead of* a boolean") is phrased with the same words that mark a rejected alternative, so priority order alone left the component section permanently empty — resolved with a `COMPONENT_API` exception; (3) three of five priority-1 hits on a real session were process talk ("since you asked to leave this on the branch rather than merge"), hence the `CONVERSATIONAL` filter. A fourth was caught by a test rather than by reading: draft filenames of the form `2026-09-16-<id>.draft.md` lead with four digits and a dash — the exact shape of an ADR filename — so drafts are now prefixed `session-`, with a guard that throws on any ADR-shaped name. Safety contract (no mutating git command, writes only inside `decisions/proposed/`, gitignored, non-ADR filename) is pinned by tests in a new `scripts` Vitest project (Node, no browser — the existing `unit` project runs real Chromium for components that touch browser APIs at mount). Suppression is deliberate: no draft when signal is below threshold or the session already wrote an ADR, since a folder with a draft per session gets ignored within a week. Logged as `decisions/0009`; `governance-audit` run before committing. `npm run validate` green (252/252, up from 234). |
| 2026-09-15 | — | **Migrated `Card`/`Dialog`/`Drawer`/`EmptyState` titles onto the `Heading` primitive internally — backlog's top-priority item closed, on `refactor/heading-migration`, not yet merged.** Verified against `tokens/brands/base/light.json` rather than assumed: `font-size-lead` and `font-size-h4` both resolve to the same primitive (`{font-size.lg}`), so all four titles map cleanly to `level={4}` — no new level needed, nothing to stop and ask about. `CardTitle`'s pre-existing `as`-only API turned out to already be decoupled from visual size (size was always fixed regardless of `as`), so it reconciled with `Heading`'s `level`/`as` split directly: `level` pinned to `4`, `as` forwarded through with its existing `'h3'` default, always passed explicitly so `Heading`'s own as-defaults-to-level never fires. Same shape for `EmptyState`'s existing `level` prop (2–6) — it already only chose the semantic tag, not the size, so it now maps straight onto `Heading`'s `as`. Two real style mismatches found by checking computed values, not assumed identical: `Card`/`Dialog`/`Drawer` titles use `font-weight-title` (bold/700) where `Heading`'s base `.heading` class hardcodes `font-weight-heading` (semibold/600); `EmptyState`'s title never set `letter-spacing` (implicit `normal`) where `Heading` always applies `letter-spacing-heading` (-0.02em). Both fixed with additive, higher-specificity compound selectors (e.g. `.heading.card__title { font-weight: var(--font-weight-title) }`) rather than editing each title's existing self-sufficient rule — `.dialog__title` specifically had to stay untouched because `AlertDialog.tsx` (out of scope) reuses that exact class directly, confirmed via grep before touching anything. `Dialog`/`Drawer` render their title through Radix's `Dialog.Title` (real `<h2>`, auto-wired `aria-labelledby`) inside `BaseSheet.tsx` — composed `Heading` via Radix's `asChild` (same pattern already used for the trigger) with `as="h2"` to keep both the tag and the ARIA wiring exactly as before. `EmptyState.css` also dropped a stray primitive-token reference (`--font-weight-semibold`, not actually caught by `no-primitive-tokens` since that rule only checks color/space/font-size/line-height, but inconsistent with the semantic layer anyway) now that `Heading` supplies the identical 600 weight via `font-weight-heading`. Confirmed byte-identical, not just "should be": screenshotted every affected story (`Card` default, `Dialog` default, `Drawer` default, `EmptyState` default/compact/all-variants, plus `AlertDialog` default as a control for the shared-class risk) via a throwaway Playwright script against local Storybook, comparing stashed pre-migration renders against post-migration ones — every pair came back byte-for-byte identical PNGs (`cmp` exit 0), computed styles (font-size/weight/letter-spacing/line-height/family) matched exactly, and `aria-labelledby`/`role` on `Dialog`/`Drawer` content stayed wired to the title's id. `npm run validate` run after each component individually (not just at the end) and green throughout (232/232 tests, 0 lint/contrast violations each time). Only DOM change anywhere: each title gained two additional classes (`heading`, `heading--h4`) alongside its existing BEM class — additive, nothing renamed or removed. Removed the backlog item; committed on the branch (`42f01ba`) for the user's review rather than merged. |
| 2026-09-15 | — | **Code review on the Heading migration branch found and fixed 3 real issues; local Storybook re-verification confirmed the fixes stayed byte-identical.** `/code-review` plus a multi-angle background review (converging independently on the same top finding) caught what `npm run validate` couldn't: (1) `registry/dialog.json`/`drawer.json` never picked up `heading.json` as a `registryDependency`, even though both compose `Heading` via `BaseSheet.tsx` — root cause was `scripts/build-registry-manifests.mjs`'s `findSiblingImports` only scanning a component's own `.tsx` and filtering out internal siblings (`BaseSheet`) entirely, so the `Dialog`/`Drawer` → `BaseSheet` → `Heading` chain was invisible to it; a registry-CLI install of either would have shipped source with an unresolvable import. Fixed the generator itself (never hand-edited the registry JSON) to walk through internal siblings while stopping at the first *public* dependency on each path — an earlier version of the fix flattened the full transitive closure instead and wrongly added `spinner.json` to `EmptyState`/`DataTable` (Button's own dependency, already covered by the CLI resolving `button.json` itself), caught by diffing the regenerated output before committing. (2) `tokens/changelog.json` was stale by exactly the chicken-and-egg gap the backlog's Self-healing CI entry already documents (a commit can't describe itself) — resolved once the migration commit existed in history to regenerate against, verified with the exact command `chromatic.yml` gates on (`changelog-sync.mjs --check`). (3) `Card`/`Drawer`/`EmptyState`'s title CSS still duplicated six properties Heading's own rules already supplied identically — trimmed to just the one property each still needs (font-weight, or letter-spacing for `EmptyState`); `Dialog`'s base rule stayed fully intact for `AlertDialog`'s sake but gained an explicit font-size pin alongside the existing font-weight one, closing the same equal-specificity tie risk the review flagged as inconsistently guarded. Re-ran the same before/after Playwright screenshot diff from the original migration across all five affected stories — every pair still byte-identical. Two lower-severity suggestions from the review (a `weight` prop on `Heading` to remove the compound-selector pattern entirely; whether this migration warranted its own ADR) were deliberately not acted on — both are judgment calls beyond this task's scope (an API change to a primitive `decisions/0008` already fixed, and a call on `decisions/README.md`'s bar that cuts either way) and are surfaced to the user rather than decided unilaterally. Also added a regression test (`BaseSheet.title.test.tsx`) for `aria-labelledby` wiring surviving the `Heading`-via-`asChild` composition — the same risk shape `decisions/0006` was written for, previously uncovered. `npm run validate` green throughout (234/234, up from 232); `changelog-sync.mjs --check` and `check-generated-sync.mjs` both clean post-commit. Committed as `75cf9b5`, `8553aa2`, `db4ed08` on the same branch, still not merged. |
| 2026-09-15 | — | **`Heading` primitive (H1–H6) merged to `main`** (PR #22, `feat/heading-primitive`) — the new component plus `decisions/0008`'s Productive/Expressive typography split. `Chromatic / chromatic` failed on first push over a genuinely stale `tokens/changelog.json` (the new `font-size-h5`/`h6` tokens weren't reflected) — fixed with `npm run tokens` + a follow-up commit, went green on rerun. Chromatic's own visual-regression check (`UI Tests`) never ran at all — the account had hit its monthly billed snapshot limit ("Build did not run because anto-amezquita's account was over the monthly limit"), confirmed on Chromatic's own build page, unrelated to anything in this PR. Branch protection isn't enabled on this repo, so the merge button stayed live despite the pending check. Substituted a manual review of the `Heading` stories in local Storybook in place of the automated visual diff before merging — judged low-risk (new component, `Card`/`Dialog`/`Drawer`/`EmptyState` deliberately untouched per `0008`) but recorded here as a real gap, not silently treated as equivalent to the automated check. Also opened `backlog.md`'s new top-priority item (migrating `Card`/`Dialog`/`Drawer`/`EmptyState`'s inline title markup onto this primitive internally) in the same session, before this merge. No npm release yet — a changeset exists on the branch per the `0008` implementation plan but publishing wasn't part of this session.
| 2026-09-15 | — | **On-demand test of Self-healing CI Phase 1, confirming it still works as designed.** Deliberately introduced real drift on a throwaway branch (`test/self-heal-drift`) — edited `tokens/global.json`'s `space.1` from `4px` to `3px` directly, committed without running `npm run tokens`, confirmed via `git diff main --stat` that only the source token file changed (not any generated artifact). `amez-ds-self-heal` (run #17) fired on push, ran detect → regenerate → PR in 39s, all green, and opened PR #20 (`chore(tokens): regenerate stale artifacts`) scoped to the branch — matching the acceptance criteria verified when Phase 1 first shipped (2026-09-10 entry below). This was prompted by a check on whether the same day's `space-scale-expansion` and `toast-tooltip-nesting-z-index` work (both entries below) had exercised self-healing — they hadn't, since both went through the normal `npm run tokens && npm run validate` flow and left nothing stale for the bot to react to; this test closed that gap directly instead. Branch is throwaway, not merged.
| 2026-09-15 | — | **Closed the Toast/Tooltip z-index backlog item — `Tooltip` confirmed clean, `Toast` had a real but different bug than the one being tested for.** Wrote `Tooltip.nesting.test.tsx` and `Toast.nesting.test.tsx` at the same rigor as `Select.nesting.test.tsx` (real `vitest/browser` `page` clicks, `styles/brands/base-light.css` imported directly). `Tooltip`, triggered by real hover from inside an open `Dialog`, renders on top and a real click on interactive content inside it lands cleanly — confirmed, no bug, matching the backlog's z-index reasoning. `Toast`'s first version of the test (assert the close button stays `getByRole`-reachable while a `Dialog` is open, mirroring `Select`'s contract) failed — but a raw pixel-coordinate click on `.toast__close` (bypassing the role query, clicking the DOM node directly) landed immediately with no actionability timeout, proving `--z-toast` does its job and nothing is painted over the toast. Traced the actual cause by reading `node_modules/aria-hidden`'s source: Radix `Dialog`'s focus trap calls `hideOthers()` on mount, which snapshots `document.body`'s children once and marks everything outside the modal `aria-hidden` — not specific to `Toast`, the same thing that happens to any other control outside an open dialog, and confirmed against Radix's own maintainers treating this as accepted, not a defect (`radix-ui/primitives#1171`: "since toasts shouldn't require user response, accessing the viewport isn't essential"). `aria-hidden`'s algorithm exempts anything already carrying an `[aria-live]` attribute at call time (and everything portalled after, since there's no `MutationObserver` — a one-time snapshot) — which is exactly why Radix's `Toast` already portals a separate hidden `ToastAnnounce` region straight to `document.body`, outside the hidden subtree, so the message still reaches screen readers regardless. Rewrote `Toast.nesting.test.tsx` to assert the two things actually guaranteed instead: the pixel click lands, and `getByRole('status')` (the announcer) still carries the toast's text while the `Dialog` is open. Both pass. Confirmed no current `toast()` call site sets `duration: Infinity`, so nothing is stranded behind a modal's focus trap either way. Removed the backlog item; `--z-dropdown`/`--z-overlay`/`--z-modal` now confirmed fully dead (already flagged for a future `decisions/0005`-style round). `npm run validate` green (215/215, up from 214). |
| 2026-09-15 | — | **Space scale expansion + Dialog padding fix released as `0.4.0` — live on npm.** Merged `feat/space-scale-expansion` to `main` and tested manually in Storybook first (Dialog padding stepping correctly across the 768px/1024px breakpoints). A changeset (`minor`) was written and committed separately, one push behind — required a `git pull` before it would push cleanly. A manual `npm publish` from the local machine failed with a 404, as expected: this package publishes exclusively via npm trusted publishing (OIDC) from GitHub Actions, which has no equivalent local credential to authenticate a manual publish with; no harm done, nothing was published by that attempt. The changesets bot's "Version Packages" PR (#18) merged shortly after, triggering `release.yml`'s `release` job — log confirmed `packages published successfully: @amezquita/design-system@0.4.0`, tag `v0.4.0` pushed. `npm view` briefly still showed `0.3.2` immediately after — a registry read-cache lag, not a failed publish — confirmed genuinely live by querying the registry API directly for the `0.4.0` version doc. |
| 2026-09-15 | — | **Closed the stale "ship the pending release" backlog item — already shipped.** Its own text said `decisions/0007`'s work sat on a branch with no changeset queued and no published npm version contained it, with "PR review, merge, then the release PR merged separately" as the remaining work. Checking `main`'s actual history found both had already happened: PR #16 (the `0007` branch) merged, then the changesets bot's "Version Packages" PR (#17) also merged (`03d2d54`, `58cfb4e`) — which per `.github/workflows/release.yml` runs `npm run release` (an actual `npm publish`) on merge, not just a version bump. Confirmed rather than assumed: `package.json` reads `0.3.2`, `CHANGELOG.md` has the matching `0.3.2` entry, `.changeset/` is back to empty (just `README.md`/`config.json`), and `npm view @amezquita/design-system version` against the real registry returns `0.3.2` — so the release is live, not just merged locally. Removed the item from `backlog.md`; no code changed. |
| 2026-09-15 | — | **Space scale expanded from 10 to 13 steps, `.dialog__content` padding made responsive.** `tokens/global.json`'s `space` scale renumbered in full (`4/8/12/16/20/24/28/32/40/48/64/96/128`, `space.1`–`space.13`), filling the gap past 16px the old scale had (no 20px, no 28px). Repointed all 14 existing references across `tokens/components/*.json` and `tokens/brands/base/light.json` to the new step holding the same pixel value (e.g. old `space.6`=32px → new `space.8`) — mapping shown to and confirmed by the user before applying, per the backlog item's own checkpoint. **Found two stale claims while executing this, both surfaced to the user rather than assumed:** the backlog item called `decisions/0004`'s container-padding split "unresolved," but that ADR's own "Follow-up" section had already closed it (32px group → `space-container-padding`, 48px group → `space-container-padding-lg`, both already live in `light.json` and wired into Card/Dialog/Drawer/EmptyState/Hero CSS) — so step 3 was just mechanical repointing, no new value call, confirmed with the user. Separately, the session started on `fix/0007-prop-passthrough-and-z-index` (an unrelated, already-merged branch) with the backlog item itself only an uncommitted local edit there, not committed anywhere including `main` — moved the work to a fresh `feat/space-scale-expansion` branch off updated `main` instead of mixing it into that branch, confirmed with the user first. Added three new semantic tokens (`space-dialog-padding-mobile/tablet/desktop` = `space.4`/`space.6`/`space.8` = 16/24/32px) since Style Dictionary's build has no mechanism for one token resolving differently under `@media` the way color tokens do under `[data-mode]` (mode-gating is separate CSS selector scopes, natively supported; media-gating isn't) — resolved via a real `@media` query in `Dialog.css` switching between the three tokens, rather than building new pipeline infrastructure for single-token media resolution; breakpoints (768px/1024px) and the mechanism choice both confirmed with the user first, since neither had any precedent anywhere in the codebase. `npm run tokens && npm run validate` clean (214/214 tests, 0 lint/contrast violations), rebuild confirmed idempotent (re-running produced no further diff) before committing. |
| 2026-09-14 | — | **Pre-merge code review on `decisions/0007`'s branch found and fixed a real regression the branch itself introduced, plus three narrower correctness gaps of the same shape.** A multi-angle review (correctness, altitude/architecture, a dedicated verification pass) found: (1) dropping `Dialog`'s explicit z-index to `auto` while leaving `Drawer`'s untouched broke a previously-correct tie — both were `--z-modal` (400) before this branch, resolved by DOM/mount order; afterward `Drawer` always outranked `Dialog`/`AlertDialog` regardless of order, the exact bug class this branch fixes, reintroduced for a different pair, with a real path to it (an `AlertDialog` opened from inside an already-open `Drawer`). Fixed by dropping `Drawer.css` to `auto` too. The regression test for this needed its own fix: mounting both `Drawer` (`defaultOpen`) and `AlertDialog` (`open`) in one initial render tripped an unrelated real Radix/`aria-hidden` bookkeeping edge case (two modal roots hiding-others in the same effect flush marked each other's container `aria-hidden`, including the topmost one's own) — rewritten to open the `Drawer` first and the `AlertDialog` only afterward via a real click, matching how this actually happens in an app. (2) `Badge`, `Checkbox`, `Textarea` (and `Input`, same shape, not flagged but fixed for consistency) each newly admitted a prop — `role`, `children`/`asChild`, `children` — that the component computes itself or can't structurally support, type-checking as accepted while being silently discarded or, for `Input`'s void-element `children` case, crashing React outright at mount (confirmed: the first version of that contract test crashed before being rewritten to construct-but-never-render). Each now explicitly excludes the offending key(s) via `Omit`, with a `@ts-expect-error` contract test as the actual assertion. Two lower-severity findings not fixed, tracked instead: `Toast`/`Tooltip` still keep their own z-index tiers (same potential bug class, unconfirmed either way), and `--z-modal`'s name now slightly drifts from what it governs (only `Drawer` references any z-index token at all post-fix, and even that reference is gone now too). `npm run validate` green throughout (214/214, up from 209). `decisions/0007`'s Verified section and the changeset both updated to reflect this. |
| 2026-09-14 | — | **`decisions/0007` written and implemented in full** — every primitive with a confirmed closed-prop-surface gap fixed, `className` merged everywhere, and the `Select`-in-`Dialog` z-index bug fixed. Prompted by a real external consumer: Ajar (a second app on this design system) ran a full migration off raw HTML controls and hit five real defects, three of them not just Button's already-fixed one — cross-checked against Radix, shadcn/ui, Base UI, React Aria, and Material UI's own approaches first (research: [The Passthrough Problem](https://claude.ai/code/artifact/23a5c026-2822-4711-bb51-358751118910)) before deciding how to fix them here, rather than improvising per-component. `Textarea`/`Badge`/`Checkbox`/`Input` all extended to `0006`'s established pattern (native-element props type, `forwardRef`, `...rest` spread), each with its own contract test — `Badge` needed its own structural approach (native attrs intersected at the *outer* union, not each branch) to avoid the doc-generator's union-of-intersections collapse `0006`'s own Alternatives section had flagged as a risk for a different component; confirmed by actually running `build-component-docs.mjs` and reading the output, not assumed either way. `Button` and every newly-fixed primitive now merge a consumer's `className` via `lib/cn.ts` instead of silently discarding it — a gap `0006`'s own fix had left open (`{...rest}` spread `className` in, then the component's own literal `className={className}` immediately overwrote it). The z-index fix took two tries: removing only `Select.css`'s `--z-dropdown` was the obvious first move and a real regression test proved it **still broken** — an explicit z-index on a sibling (`Dialog`'s `--z-overlay`/`--z-modal`) always outranks an `auto` one regardless of DOM order, so both sides needed to drop to `auto` before Radix's own "portal mount order handles it" behavior actually took effect. That regression test itself needed two fixes before it was trustworthy: `@testing-library/user-event`'s `.click()` doesn't do real hit-testing and passed against the *broken* CSS with zero changes, so it was rewritten against `vitest/browser`'s `page` locator API (real Playwright actionability checks) — which then reproduced Ajar's exact failure string (`<div class="dialog__overlay"> intercepts pointer events`) before the fix and went green after; and the `unit` Vitest project loads no brand CSS by default (unlike Storybook), so the test needed `styles/brands/base-light.css` imported directly or the z-index tokens it was testing never resolved to real numbers at all. `npm run validate` green throughout (209/209 tests, up from 196; 0 lint/contrast violations); `npm run tokens` regenerated `docs/components/{badge,checkbox,textarea}.md` and confirmed `--z-dropdown`/`--z-overlay` now fully dead in `tokens/dependency-graph.json`, left defined rather than deleted (flagged in `backlog.md`). Also found and fixed, independently: `decisions/0006`'s own fix (the `...rest` spread + `AlertDialog`→`Button` switch) was merged to `main` 2026-09-08 but never got a changeset — no published npm version contains any of it, confirmed against `CHANGELOG.md` and `.changeset/`. Not yet released — see `backlog.md`. |
| 2026-09-10 | — | **Self-healing CI Phase 1 shipped and verified end to end** (`specs/self-healing-ci-spec.md`). GitHub App `amez-ds-self-heal` created, installed on this repo only, scoped to Contents + Pull requests read/write. All five acceptance criteria pass against real runs. The one that mattered: `chromatic` runs *unattended* on the bot's PR and goes green — verified against a deliberate `GITHUB_TOKEN` control run the same morning, which confirmed the documented behaviour firsthand (the run is created but frozen at `action_required`, never starting without a human click). That control was worth running: it also corrected the spec's claim that such a PR "silently skips every check" — it isn't silent, it's a visible frozen banner, still unattended-hostile. **Two real bugs found by acceptance testing that review had missed, both traceable to the spec rather than the implementation.** The bot healed the component docs but not `tokens/changelog.json`, leaving a drifting branch still red and the human still running `npm run tokens` — the whole trip Phase 1 exists to save; fixed with `scripts/changelog-sync.mjs` owning the content-only comparison (`generatedAt` nulled) in both directions, shared with `chromatic.yml` so the two can't disagree. And the spec's sketch said "full history not needed", so the default depth-1 checkout left `build-changelog.mjs` seeing 1 eligible commit and 0 tags against 69 — the bot committed a changelog with 220 lines deleted; fixed with `fetch-depth: 0`, diagnosed by cloning at `--depth 1` and running the builder's own git query rather than reasoning about shallow clones. Both failures produced *correctly scoped* PRs that were still unmergeable, which is why the acceptance bar here is "merges green and leaves the branch clean", not "commits the right files". All testing done in throwaway branches, since removed; merged to `main` as PR #13. Still open, both in `backlog.md`: watching the bot's real-world PR rate before adding rules on top of it (the changelog is stale-by-one after *every* qualifying commit, so the bot fires on nearly every component branch — useful or noisy is not yet knowable), and branch protection, gated on deciding whether `update-changelog` gets a bypass or gets reworked to open a PR like everything else. |
| 2026-09-10 | — | Built the two code steps of self-healing CI Phase 1 (`specs/self-healing-ci-spec.md`, whose own session log carries the detail). Extracted the generated-artifact path list — previously written out twice inside `chromatic.yml`, and about to become four copies — to `scripts/generated-artifacts.mjs`, the one place it now lives; `chromatic.yml`'s staleness step collapsed to `node scripts/check-generated-sync.mjs`, and the new workflow resolves its `add-paths` from the same module rather than restating the paths in YAML. Verified the new check catches both drift shapes (a modified generated file, and a brand-new untracked one — the case plain `git diff` is silent about), and ran the new workflow's PR-body step verbatim against simulated drift instead of assuming the shell was right. Wrote `.github/workflows/self-heal-stale-artifacts.yml` with three deliberate divergences from the spec sketch, all recorded there: `contents: read` rather than `contents: write` + `pull-requests: write` (every write goes through the GitHub App token, so GITHUB_TOKEN needs only what checkout uses), `delete-branch: true` (what actually satisfies the spec's own branch-cleanup acceptance criterion), and `client-id` rather than `actions/create-github-app-token`'s deprecated `app-id`. Both third-party actions pinned to resolved commit SHAs, fetched live rather than recalled. **Two pre-existing governance divergences found by the audit pass and fixed:** `docs/quality.md`'s `npm run validate` chain omitted `tokens:lint-architecture` (added 2026-09-08 and never reflected), and `AGENTS.md` omitted both that and the test suite — exactly the docs-contradict-code shape `contributor-skills/governance-audit` exists to catch, and invisible to `npm run validate` itself. Steps 1, 2 and 3 of the backlog list are all GitHub-UI work and stay open; the workflow must not be pushed before the App secrets exist. `npm run validate` clean, `npm run tokens` produces no diff. |
| 2026-09-08 | — | Closed both remaining items on `backlog.md`'s token architecture list (removed that section entirely — see `backlog.md`'s protocol). Item 3 ("stop the growth"): shipped `scripts/lint-token-architecture.mjs` + `npm run tokens:lint-architecture`, wired into `validate` — gates any new component token that resolves identically to its referent in every mode, matching `audit-tokens.mjs`'s own passthrough/chain-skip definitions but reading token sources directly rather than the built `token-reference.json`. Ran it against the live repo and found `light.json`'s `font-size-label` semantic role had the same bug shape `decisions/0004` found in `font-weight-label`: it existed, was unused, and was set to the wrong value (`font-size.xs` instead of `.sm`). Fixed it and collapsed `input-label-size`, `label-font-size`, `textarea-label-size`, `select-label-size` all onto it (`decisions/0005` rounds 24–25) — all four form-label components now share one role for both size and weight. Tagged the 71 remaining chain-skips `decisions/0004` deliberately left below its 3+-shared-function bar with `$extensions` ignore reasons, one per token. Item 4 (pass-through collapse): turned out to already be done — `decisions/0005`'s 23 rounds (predating this session) had already taken passthroughs to 0; the backlog note was just stale and is now corrected. Along the way, tried and reverted two approaches to get Select's dropdown-content under Chromatic visual coverage (a play-function click, then a `defaultOpen` prop) — both got captured as closed, because Radix Select closes on window resize and Chromatic resizes the viewport as part of its own capture step, regardless of how the select was opened. Verified the Select label-size fix manually in local Storybook instead of forcing a misleading automated check; documented the limitation in `decisions/0005` rather than silently dropping it. `npm run validate` and `npm run tokens` clean throughout. |
| 2026-09-07 | — | Split the session-anchor role out of this file: [`backlog.md`](./backlog.md) is now the file to open first (Session protocol + the two live backlog items moved there); this file is pure history from here on. Updated every cross-reference pointing at the old "roadmap.md is the anchor"/"token-architecture backlog in roadmap.md" claims — `AGENTS.md`, `architecture.md`, `quality.md`, `ai-readiness.md`, `ai-readiness-audit.md` (also fixed its stale pointer to the full task-by-task record, which actually lives in `roadmap-archive.md` since the 2026-09-07 trim, not here), `specs/docs-site-spec.md`, and `contributor-skills/design-system/SKILL.md`. Also fixed one pre-existing stale reference unrelated to this restructuring: `contributor-skills/README.md` pointed at a file named `docs/ai-readiness-plan.md`, which was renamed to `roadmap.md` back on 2026-09-02 and never updated. **Found while moving the Token architecture backlog into `backlog.md`:** its numbers (196 pass-through / 164 chain-skip candidates) were stale — `decisions/0004` had already run five rounds collapsing chain-skips down to 74, closing most of that item, which the original backlog text didn't reflect. Corrected in `backlog.md` rather than carried forward silently; the pass-through count (196) is confirmed still untouched by 0004's scope and remains genuinely open. Did not run `contributor-skills/governance-audit` before these edits (no way to invoke a contributor skill from this session) — flagged to the user as a gap, per `AGENTS.md`'s own "Do not edit AGENTS.md/architecture.md/quality.md without running governance-audit first" rule. `npm run validate` not run either, for the same reason (no shell access to this repo from this session) — both are the user's to run before committing. |
| 2026-09-02 | — | Shipped layered filtering on `DataTable` — column-level filters plus an app-owned `renderToolbar` slot, the biggest documented gap against the dense-data research at `amezquita.dk/dashboard-ux-patterns`. Full process, open-question decisions, and cold-test detail in the new [`specs/layered-filtering-spec.md`](../specs/layered-filtering-spec.md), summarized here per this file's own role as the roadmap anchor. Audited `DataTable.tsx`'s selection logic before writing anything: selection was `Set<number>` over positions in the sorted array, kept safe only because sorting explicitly clears it — migrated it to identity-keyed `Set<string>` (`getRowKey ?? id ?? stable data-position`) so a selected row now survives being filtered out and back in, without changing sort's existing clear-on-change behavior or the render `key` prop's own logic. Reused existing mechanisms throughout rather than inventing new ones: `Input`(search)/`Select` for the filter row's controls, and `useUncontrolledValue` generalized from `string`-only to generic so the new `filters`/`onFiltersChange` dual-mode state rides the same controlled/uncontrolled path `Input`/`Textarea` already use. Found and fixed a real generator bug while implementing, not before: `build-component-docs.mjs`'s `expandObjectAliases()` does a bare-word regex substitution with no awareness of comments vs. type syntax, so a first-draft doc comment starting with "Column-level filter…" collided with the `Column<T>` alias name being expanded and corrupted `get_component("DataTable")`'s own `columns` prop type — fixed by rewording rather than touching the shared generator, since the collision was self-inflicted wording, not a systemic gap. Since this repo has no unit-test framework, the review gate ran against a real headless browser instead of trusting a self-assessment: a throwaway Vite dev server plus Playwright (both already project dependencies) drove four live `DataTable` instances through 22 real interaction assertions — filter-then-sort and sort-then-filter converging on byte-identical final row order, a selected row surviving a filter-then-clear cycle while a different selected row stayed visible throughout, and a zero-filterable-columns table behaving exactly as before. Cold test (`InventoryTable.tsx`, text + select filters, a toolbar search box, MCP tools only) ran clean twice in a row — both fresh subagents found `filterable`/`filterType`/`filterOptions`/`renderToolbar` from `get_component` output with zero guessing. One new, real, out-of-scope finding surfaced by that cold test and logged rather than fixed here: `get_component("DataTable")`'s doc doesn't surface the `T extends Record<string, unknown>` generic constraint on the `render` prop, so a plain row interface fails to type-check against it unless it happens to carry an index signature — pre-existing, unrelated to filtering, a candidate for a future `build-component-docs.mjs` pass. `npm run validate` clean after every step. |
| 2026-08-12 | — | Found a second, distinct symptom of the portfolio's known token-pipeline drift (see the Phase 1/3 entry above). `amezquita.dk/design-system` — the live docs overview page, `app/design-system/page.tsx` — correctly derives its stats from a token-reference.json import rather than hand-typing them (`compiled, not written` done right), but the file it imports is portfolio's own local `tokens/token-reference.json`, not the published package. That local file still has the old flat meta shape and reports **571 tokens**, while this repo's canonical count is **620** — so the case-study page (`620`, hand-typed prose, correct as written) and the docs overview page (`571`, auto-derived, stale source) now visibly disagree on the same site. Component count is unaffected — portfolio's local `component-registry.json` independently says the correct 28. The earlier-logged decision ("full pipeline duplication... judged too invasive for a live site") still stands as the right call for the `public/` agent-facing snapshot; this is a different surface (a live page's direct import, not a static copy) and hasn't been decided yet. Likely fix, not attempted: point `app/design-system/page.tsx`'s import at `node_modules/@amezquita/design-system/tokens/token-reference.json` instead of the local copy. A third surface shares the same root cause without the same symptom: `/design-tokens` (`app/design-tokens/page.tsx`) renders from `tokens/tokens-manifest.json`, also generated from the local copy — it displays no total count, so nothing there visibly contradicts, but it's a curated set (70 entries: 8 typography, 7 spacing, 7 motion, 4 radius, 2 opacity, 42 color) that could be silently missing whatever's part of the same 49-token gap, rather than showing it wrong. Not diffed to confirm which tokens, if any. **Held at Antonio's request — job search is the priority; logged so it isn't lost, not fixed.** |
| 2026-09-02 | — | Adopted the ai-product-starter-kit's governance conventions repo-wide, prompted by the user noticing this repo lacked the `/decisions` (ADR) folder the kit itself establishes as the standard. Not a phase of this plan — a structural change to where governance content lives. Created `/decisions` (kit's ADR template, copied verbatim) and wrote 3 ADRs: `0001` (the base/portfolio brand split from earlier the same day), `0002` (rejecting transitions.dev as a library while keeping its audit technique), `0003` (the new `no-hardcoded-line-height` lint rule and the 5 fixes it caught). Moved the 4 existing feature specs (`mcp-server-spec.md`, `compound-component-docs-spec.md`, `layered-filtering-spec.md`, `docs-site-spec.md`) from `docs/` to a new `/specs`, and fixed every cross-reference to them across this file and the specs themselves — including relative links that broke on the move (`./mcp-server-spec.md` → `../docs/ai-readiness-plan.md` from inside `specs/`, and the reverse direction here). Wrote `docs/architecture.md` and `docs/quality.md` with real content adapted from the kit's product-oriented templates — sections assuming a backend/product (auth, data architecture, brand.md, content.md) marked N/A rather than left as unfilled brackets, since this is a component-library package. Created `contributor-skills/` (not `/skills` — that name is already taken by the generated, consumer-facing `skills/amezquita-design-system/` that `npm run tokens` rebuilds; collision avoided deliberately) with two skills: `design-system` (adapted from the kit's own, trimmed of product-only sections) and `token-audit` (a new skill capturing the actual technique used for this session's duration and line-height audits, so it's repeatable next time rather than re-derived). Rewrote `AGENTS.md` (and its symlink `CLAUDE.md`, confirmed still a real symlink — the edit propagated automatically) to point at all of the above, plus a new "Do not" section adapted from the kit's convention (hand-editing generated files, adding pass-through component tokens without reason, a second full semantic tier per brand, adopting a library wholesale for one technique, skipping an ADR for an architectural change). Ran `npm run validate` after the AGENTS.md rewrite — clean, but its own output surfaced a real, unrelated inconsistency: `check-components-doc.mjs` printed "29 components documented" against `AGENTS.md`'s stated "28 public components." Investigated before fixing anything — not a bug in the data (confirmed against `tokens/component-registry.json`: `publicComponentCount: 28`, `componentCount: 50` including 21 compound sub-components from the 2026-09-02 compound-component-docs work), just two scripts counting different things (all documented directories including internal-only `BaseSheet`, vs. public components only) with no distinction in the message. Fixed `check-components-doc.mjs` to compute and report both numbers explicitly rather than one raw figure. `npm run validate` re-run clean, message now reads "29 directories documented (28 public + 1 internal)." Everything from this session committed in one commit. |
