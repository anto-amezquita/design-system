# TabsTrigger

> Sub-component of Tabs.

- Tier: patterns
- Storybook: `Components/Tabs`
- Import: `import { TabsTrigger } from '@amezquita/design-system/components/patterns/Tabs'`

Also accepts all props of: `React.ComponentPropsWithoutRef<typeof RadixTabs.Trigger>`

## Usage example

See `Tabs`'s own usage example — TabsTrigger is one of its sub-components, not used standalone.
