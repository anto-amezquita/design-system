# CardTitle

> Sub-component of Card.

- Tier: composition
- Storybook: `Components/Card`
- Import: `import { CardTitle } from '@amezquita/design-system/components/composition/Card'`

## Props

| Prop | Type | Description |
|---|---|---|
| `children` | `React.ReactNode` |  |
| `as?` | `'h1' \| 'h2' \| 'h3' \| 'h4' \| 'h5' \| 'h6'` |  |

## Usage example

See `Card`'s own usage example — CardTitle is one of its sub-components, not used standalone.
