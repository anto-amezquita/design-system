# TableCell

> Sub-component of Table.

- Tier: patterns
- Storybook: `Components/Table`
- Import: `import { TableCell } from '@amezquita/design-system/components/patterns/Table'`

Also accepts all props of: `React.ComponentPropsWithoutRef<'td'>`

## Usage example

See `Table`'s own usage example — TableCell is one of its sub-components, not used standalone.
