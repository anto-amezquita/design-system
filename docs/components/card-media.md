# CardMedia

> Sub-component of Card.

- Tier: composition
- Storybook: `Components/Card`
- Import: `import { CardMedia } from '@amezquita/design-system/components/composition/Card'`

## Props

| Prop | Type | Description |
|---|---|---|
| `children` | `React.ReactNode` |  |

## Usage example

See `Card`'s own usage example — CardMedia is one of its sub-components, not used standalone.
