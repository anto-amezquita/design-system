# TableHead

> Sub-component of Table.

- Tier: patterns
- Storybook: `Components/Table`
- Import: `import { TableHead } from '@amezquita/design-system/components/patterns/Table'`

Also accepts all props of: `React.ComponentPropsWithoutRef<'thead'>`

## Usage example

See `Table`'s own usage example — TableHead is one of its sub-components, not used standalone.
