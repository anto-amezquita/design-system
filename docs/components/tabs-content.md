# TabsContent

> Sub-component of Tabs.

- Tier: patterns
- Storybook: `Components/Tabs`
- Import: `import { TabsContent } from '@amezquita/design-system/components/patterns/Tabs'`

Also accepts all props of: `React.ComponentPropsWithoutRef<typeof RadixTabs.Content>`

## Usage example

See `Tabs`'s own usage example — TabsContent is one of its sub-components, not used standalone.
