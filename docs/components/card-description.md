# CardDescription

> Sub-component of Card.

- Tier: composition
- Storybook: `Components/Card`
- Import: `import { CardDescription } from '@amezquita/design-system/components/composition/Card'`

## Props

| Prop | Type | Description |
|---|---|---|
| `children` | `React.ReactNode` |  |

## Usage example

See `Card`'s own usage example — CardDescription is one of its sub-components, not used standalone.
